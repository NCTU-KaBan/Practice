#include <bits/stdc++.h>
using namespace std;
int main() {
    int n, m; cin >> n >> m;
    for (int i = 0 ; i < n ; i++) {
        int a, b; cin >> a >> b;
    }
    for (int i = 0 ; i < m ; i++) {
        int a, b; cin >> a >> b;
    }
    if (n == m) cout << "Yes\n";
    else cout << "No\n";
}
